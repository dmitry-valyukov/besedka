#pragma once
// Две вкладки, за которыми пока ничего нет.
//
// Порт `ui/screens/WatchedScreen.kt` и `ui/screens/OutboxScreen.kt` из jana:
// там это тоже заглушки -- надпись посреди пустого места. Стоят они здесь по
// той же причине, по какой стоят у неё: нижняя панель показывает, из чего
// приложение состоит, и вкладка, которой нет, врёт об этом сильнее, чем
// вкладка без содержимого.
//
// Что за ними появится, написано на них же -- чтобы пустой экран не выглядел
// поломкой.

#include "pch.h"

namespace besedka::app {

/// Избранное: темы, за которыми читатель следит.
wxl::UIElement watchedScreen();

/// Исходящие: написанные ответы, ещё не ушедшие на сервер.
wxl::UIElement outboxScreen();

}  // namespace besedka::app
